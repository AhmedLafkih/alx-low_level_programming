#include "main.h"

/**
 * _abs - function that cmpute the absolute value of integer
 *
 * @num: takes in integer type input for function
 *
 * Return: Always 0 (Success)
*/

int _abs(int num)
{
if (num < 0)
{num = (-1) * num;
return (num); }
return 0;
}
