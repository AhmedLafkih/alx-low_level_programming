#include "main.h"

/**
* postitive_or_negative - function that prints if integer is positive or negative
* @n :check the value pos or neg
* Return: 0
*/

int postitive_or_negative(int n)
{
        int i;

        i = 98;
        positive_or_negative(i);

        return (0);
}
